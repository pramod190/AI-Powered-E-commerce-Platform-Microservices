# Service Template (Express + TypeScript)

Reusable microservice boilerplate you can copy for each Node.js service.

## Run

```bash
npm install
npm run dev -w services/service-template
```

Health check:
- `GET /health`

